# dramatic_options runtime dependencies.
# Pinned EXACT: the system depends on alpaca-py's exact signatures (SPEC §9), so an
# SDK release must not silently change order/data semantics under us.
# chromadb is intentionally NOT here yet — it arrives in Phase 6 (TMS) when needed.
alpaca-py==0.43.4
pandas==3.0.3
python-dotenv==1.2.2
# Phase 1: numpy (backtest metrics) and requests (keyless EDGAR) are now DIRECT deps —
# pinned rather than relied on transitively (build principle: pin what you import).
numpy==2.4.6
requests==2.34.2
# T2 council: heterogeneous LLM router. Three SDKs cover all SPEC §5 providers — openai
# also serves xAI/Grok + Perplexity via base_url. Lazy-imported inside the provider adapters,
# so CI / `orchestrator.py --demo` / tests (FakeRouter) need none of these installed.
anthropic==0.105.2
openai==2.38.0
# PIN EXACT: GeminiProvider depends on google-genai 2.x field names that are version-coupled —
# `GenerateContentConfig.thinking_config` / `response_mime_type` and `ThinkingConfig.thinking_level`
# (the Gemini-3.x "minimal thinking" knob that fixes the #37 proposer thinking-starvation). A bump
# that renames these passes the offline (mocked) tests but breaks live SILENTLY → re-run the
# `@pytest.mark.live` council round-trip after any upgrade.
google-genai==2.7.0
