# dramatic_options environment — copy to .env and fill in. (.env is gitignored.)
# Mirrors the real_options convention of a per-host .env loaded by the service.

# Web app
PORT=8000
# HEALTH_URL is read by scripts/verify_deploy.sh during deploys.
HEALTH_URL=http://127.0.0.1:8000/health
