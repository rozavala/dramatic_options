# VIAV — window-#3 MAIN admission (2026-08-03)

**Operator authorization (TRUE form):** "let's admit, right? what's your suggestion?" —
2026-08-03, after the sweep record (`2026-08-03_window3_admission_sweep.md`) presented the
mechanical admission set {VIAV} for veto. Recommendation given: admit (rule-compliant twice,
three weeks apart; no restricted-list or thesis objection grounds a veto). The operator's
word closed it.

## The admission

- **Screen (the §11 mechanical pass):** `VIAV270416C00045000`, dte 256, **24.6% achieved OTM**
  (band 15–35%), **$915/contract ≤ $1,000** per-name cap, ADV $175.5M, price $36.11 —
  in-window 2026-08-03 13:47 UTC, production `run_screen` path. Reproduces the 2026-07-10
  neighbor-screen read (28.2%, $1,065 → now 24.6%, $915): a **stable expression**, unlike
  ADTN's documented day-to-day instability.
- **Register:** `universe_register.json` themes.`fiber_buildout` (provenance operator;
  machine-surfaced neighbor of the operator's staged bead_fiber_buildout thesis, Rule-3
  tagged; falsifier draft-dated ~Aug-2027 under the admission delegation) + windows.`3`.
- **Scan basket:** `config.universe.themes.fiber_buildout = [VIAV]` — the discovery universe
  goes 39 → 40 names. **This is the first act of the sentinel expansion** the operator
  approved 2026-08-03 (admitted names join the scan; the L0 funnel + council judge from the
  next scan; gates and caps dispose). The hand-seed watchlist (`themes.json`) is untouched.
- **Cluster:** NEW `fiber_capex = [VIAV]` (9th cluster) — the fiber/optical-network buildout
  capex driver (AI backbone interconnect + BEAD deployment → test-and-measurement spend).
  Deliberately NOT routed into `ai_capex_power`: that cluster's driver is AI *power*
  equipment; VIAV's is fiber *deployment* — sharing a budget would misprice both. The book
  still fills at most 5 clusters ($10k / $2k) — geometry unchanged, `cluster_fraction`
  stays 0.02.

## Discipline notes

- Additive-only; no name removed; the 11 sweep fails stand as recorded (TITN's 14.4%
  near-miss NOT admitted — no HARK loosening; HLIT/TWI/KMT tenor-floor re-open ~Sept).
- The §5 clock: window-#3 curation is the act the closed read UNLOCKED (2026-08-02) — this
  admission starts the next universe epoch; the next L0 (Sun 2026-08-09) scans 40 names.
  Any §5-style read over scan cohorts spanning this date must treat 08-03 as a
  universe-change boundary (the CC mid-clock precedent, now on the sanctioned side).
- Dated follow-up (carried from the sweep record): the IRDM ranked-union starvation —
  revisit the union-cap/rank question ≥2026-08-10 as its own operator-gated discussion.
